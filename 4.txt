[Should be done on a WINDOWS MACHINE with WIRESHARK Installed]
#Open Wireshark and start a Wireshark capture by double clicking a network interface with traffic. 
# Flush DNS cache
ipconfig /flushdns

# Start DNS query
nslookup

# Inside nslookup
www.cisco.com
exit

# (Wireshark filter for DNS traffic)
udp.port == 53

# View ARP table
arp -a

# View IP configuration
ipconfig /all

# In Wireshark Expand Domain Name System (response). Then expand the Flags, Queries, and Answers. Observe the results. 
