[SHOULD BE DONE ON CYBEROPS WORKSTATION]
# Go to PCAP directory
cd ./lab.support.files/pcaps/

# List files
ls -l

# Open PCAP in Wireshark
wireshark-gtk nimda.download.pcap

# (Inside Wireshark GUI steps)
# right click on TCP which shows top on the list. Then click on Follow TCP  Stream → Follow TCP Stream → Identify file → Export HTTP object → Save .exe

# Verify extracted file
ls -l

# Check file type
file W32.Nimda.Amm.exe
